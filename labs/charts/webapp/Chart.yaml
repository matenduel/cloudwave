apiVersion: v2
name: webapp
description: "CLOUDWAVE 실습용 미니 웹앱 차트 — nginx 공식 이미지를 Deployment/Service/Ingress(ALB)로 배포합니다"
type: application
# 차트 자체의 버전 (템플릿·기본값이 바뀌면 올립니다)
version: 0.1.0
# 이 차트가 배포하는 앱(nginx)의 버전 — image.tag를 비워두면 이 값이 태그로 쓰입니다
appVersion: "1.29.8-alpine"
