apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: card1-live-replay
  labels:
    app.kubernetes.io/name: card1-live-replay
    app.kubernetes.io/part-of: card1-observability-kit
    # kube-prometheus-stack's Helm-value selector requires this label.
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - commerce-lab
  selector:
    matchLabels:
      app.kubernetes.io/name: card1-live-replay
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
      honorLabels: true
