apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Card-specific replacement point: keep this in sync with namespace.yaml and
# the baked datasource URL in platform/card1-baked-datasource.yaml.
namespace: commerce-lab

resources:
  - namespace.yaml
  - storage
  - bake
  - platform
