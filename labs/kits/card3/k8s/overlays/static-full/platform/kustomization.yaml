apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../platform
patches:
  - target:
      kind: Deployment
      name: card3-live-replay
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 0
