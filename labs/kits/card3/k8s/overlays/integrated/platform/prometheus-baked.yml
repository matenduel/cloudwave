global:
  scrape_interval: 15s
  evaluation_interval: 15s

# Historical scenario samples are pre-baked into the mounted TSDB. Keep this
# server isolated from live replay so Grafana can compare the two datasources.
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["127.0.0.1:9090"]
